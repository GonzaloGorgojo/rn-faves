import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderColor: 'red',
    borderWidth: 2,
    width: '90%',
    marginTop: 5,
    alignItems: 'center',
  },
});
